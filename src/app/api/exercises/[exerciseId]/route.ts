import { NextResponse } from "next/server";
import { openDatabase } from "@/lib/sqlite";
export const runtime = "nodejs";
export async function PATCH(request: Request, { params }: { params: Promise<{ exerciseId: string }> }) { const { exerciseId } = await params; const body = await request.json(); const name = String(body.name ?? "").trim(); const category = Array.isArray(body.categories) && body.categories.length ? body.categories.map(String) : ["기타"]; const instructions = String(body.instructions ?? "").trim() || null; const mediaUrl = String(body.mediaUrl ?? "").trim() || null; if (!name) return NextResponse.json({ error: "운동명을 입력해 주세요." }, { status: 400 }); const db = openDatabase(); try { const duplicate = db.prepare("SELECT id FROM Exercise WHERE name = ? AND id != ?").get(name, exerciseId); if (duplicate) return NextResponse.json({ error: "같은 운동명이 이미 등록되어 있습니다." }, { status: 409 }); const result = db.prepare("UPDATE Exercise SET name = ?, category = ?, instructions = ?, mediaUrl = ?, updatedAt = CURRENT_TIMESTAMP WHERE id = ?").run(name, JSON.stringify(category), instructions, mediaUrl, exerciseId); return result.changes ? NextResponse.json({ ok: true }) : NextResponse.json({ error: "운동을 찾을 수 없습니다." }, { status: 404 }); } finally { db.close(); } }
export async function DELETE(_: Request, { params }: { params: Promise<{ exerciseId: string }> }) { const { exerciseId } = await params; const db = openDatabase(); try { const result = db.prepare("DELETE FROM Exercise WHERE id = ?").run(exerciseId); return result.changes ? NextResponse.json({ ok: true }) : NextResponse.json({ error: "운동을 찾을 수 없습니다." }, { status: 404 }); } finally { db.close(); } }
