# Local SQLite database for development. Do not commit production credentials.
DATABASE_URL="file:./dev.db"
DATABASE_PATH="prisma/dev.db"

# Required for voice transcription feature
# OPENAI_API_KEY="sk-..."

# Production example (Railway with persistent volume mounted at /data):
# DATABASE_URL="file:/data/dev.db"
# DATABASE_PATH="/data/dev.db"
